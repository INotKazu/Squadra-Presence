{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main Squadra Presence window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
